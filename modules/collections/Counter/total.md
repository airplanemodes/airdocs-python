# collections.Counter.total()

Compute the sum of the counts.

### Syntax

```python
collections.Counter.total()
```

### Examples

```python
>>> c = collections.Counter(a=10, b=5, c=0)
>>> c.total() # 15
```
